/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true, // Recommended for Next.js applications
  // Add any other Next.js configurations here if needed
  // For example, if you were using the 'app' directory, you'd add:
  // experimental: {
  //   appDir: true,
  // },
};

module.exports = nextConfig;
